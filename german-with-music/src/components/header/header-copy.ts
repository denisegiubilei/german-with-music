/** Resolved strings passed from the server `Header` into the client navbar shell. */
export type HeaderCopy = {
  brand: string;
  navSongs: string;
  navLibrary: string;
  navHowItWorks: string;
  navStart: string;
  languageAriaLabel: string;
  themeAriaLabel: string;
  themeLight: string;
  themeDark: string;
};
